import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StuffChangePassPage } from './stuff-change-pass';

@NgModule({
  declarations: [
    StuffChangePassPage,
  ],
  imports: [
    IonicPageModule.forChild(StuffChangePassPage),
  ],
})
export class StuffChangePassPageModule {}
